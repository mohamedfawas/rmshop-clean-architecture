-- If you need to revert, you can set it back to the default
-- or to a specific timezone. Here we're using the server's default:
SET timezone TO DEFAULT;
ALTER DATABASE rmshop_db SET timezone TO DEFAULT;